apiVersion: v1
kind: Pod
metadata:
  name: redis-pod
spec:
  containers:
    - name: redis
      image: redis
      volumeMounts:
        - mountPath: /data
          name: redis-storage
          mountPath: /data/redis
  volumes:
  - name: redis-storage
    emptyDir: {}

